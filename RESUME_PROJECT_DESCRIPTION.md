# 单项目简历描述（整合版 v5）—— 麻将AI助手

> 使用说明：正文即最终投递版；文末"面试追问应答"仅备面试使用，不放入简历。

## 【项目描述】

面向麻将实时对局「牌面小难辨识、对手手牌不可见、经验判断易失误」三大痛点，构建了基于计算机视觉与概率推断的实时辅助决策系统：自动重建牌局状态、概率化推断三家对手的听牌与放铳威胁，并输出牌效最优的出牌建议（纯只读辅助，不干预游戏）。

## 【核心技术栈】

`Python 3.12` `PyTorch(CUDA)` `YOLOv8` `ByteTrack` `粒子滤波` `Beam Search` `OpenCV` `PySide6` `异步任务流水线` `pytest/mypy/ruff`

## 【核心职责与成果】

1. **主导**攻克小目标识别难题：牌河 15px 级牌面模糊难辨、通用检测器召回不足（74%），落地级联检测策略（Cascade Detection）——低阈值全图召回 + ROI 放大特写分类，高置信框直达输出、低置信走细分类，牌河召回率提升至 85%、精度 98%，单帧耗时压缩 20%，支撑 14fps 稳定刷新。

2. **攻克**对手手牌不可观测的推断难题：仅能通过"打出/未打出"反向估计——以粒子滤波（3 家 × 400 粒子，剩余牌池采样满足张数守恒）近似后验，用向听数边际价值刻画理性玩家行为，叠加软观测（概率权重注入观测，容忍归属歧义）与快照回放抵御噪声，手牌推断与真实手牌吻合度约 95%（真值对局验证），听牌概率与放铳风险量化上屏。

3. **重构**感知链路为事件驱动架构：针对单帧检测漏检/误检/抖动的固有缺陷，确立"检测输出仅为候选信号"原则——多信号交叉确认沉淀可靠事件流，轮转顺序约束（Turn-order Constraint）+ Beam Search 联合解码"谁打了什么"（Beam Size=16，窗口=最近 10 次出牌事件，辅以牌河落点似然与跳家惩罚剪枝），证据不足保留软归属、快照回放精确修正；检测异常场景下状态重建自洽、不连锁错位。

4. **支撑**端到端实时体验：异步任务流水线 + 背压削峰（最新帧优先、队列上限保护），重计算全部下沉后台、画面解析与决策互不阻塞，端到端决策延迟稳定在 120~180ms，最坏情况有界 < 1s。

5. **沉淀**工程化质量闭环：363 项自动化测试全绿（pytest + mypy strict + ruff 零告警），自建「采集-标注-训练-评估-基准」闭环，以实战录像离线基准持续回归，单帧识别耗时 86.6ms → 69.6ms。

---

## 面试追问应答（不放入简历）

**Q1：推荐计算只要 20ms，端到端却要 120~180ms，中间时间去哪了？**

A：10~20ms 只是"出牌推荐"这一个纯计算步骤（向听数 + 有效进张枚举，带结果缓存）。120~180ms 的预算主要在**感知侧而非决策侧**：
- 视觉识别约 70ms（优化后均值：检测 + 跟踪 + ROI 精修；真实对局含异常帧时 P50 约 123ms，这正是"最坏有界 < 1s"的由来）；
- 截屏约 28ms，与流水线同步、多帧事件确认合计约 50ms（新轨迹 2 帧稳定、手牌去抖 5 帧窗口，跨帧并行于识别，非简单累加）；
- 推断在后台线程异步执行（缓存命中时 10~20ms），不占主循环。

也正因如此，优化检测才有意义：ROI 逐框精修曾是帧率瓶颈（曾把帧率从 5.4 拖到 1.1fps），优化后 ROI 精修 24.9ms → 5.0ms，帧率由 11.5fps 提升至 14fps。

**Q5：Beam Size 为什么是 16？怎么保证解码不跑飞？**

A：解码只对窗口内未冻结的至多 10 个事件重解（出窗即冻结），每个事件 4 家分支、束宽 16 剪枝——重解复杂度有界（O(窗口 × 束宽 × 4)），实测毫秒级；空间落点似然与跳家惩罚作为剪枝信号参与束内排序，联合解空间不会指数爆炸。窗口 k=10 与束宽 16 均来自真实对局调参（事件间隔 3~13s，窗口 10 约覆盖 1~2 巡，出窗冻结保证末张也能收敛）。

**Q2：置信度阈值为什么选 0.8？**

A：40 帧实测中 conf≥0.8 的框占 96.5%，其中仅 2.8% 会被精修改判类别（低置信框 25% 需精修）——说明高置信框类别已可靠，跳过精修几乎无损（分级 vs 全量一致率 97.3%），ROI 成本从每帧全部框降到 ~3.5% 的框。

**Q3：快照回放如何保证确定性？**

A：每个观测应用前拍摄快照（粒子表 + 牌池 + RNG 状态 + 手牌张数，约 0.4ms）；归属翻案时回退到该观测快照，按新归属分布确定性重放后续全部观测，RNG 随快照恢复，因此回放结果可复现、可单测。

**Q4：向听数边际价值的 α 惩罚系数怎么定的？**

A：Δ = 打出后向听数 − 打出前向听数，Δ>0 表示打出毁牌效。α=1.2 时 Δ=1 权重 ×0.30、Δ=2 ×0.09——明显抑制"打坏牌"的粒子，但保留防守拆牌/失误的余地（玩家不完全理性）；叠加 exp(−β·向听数) 结构化漂移模拟"朝听牌构建"（β=0.4，β=1.0 实测更差：假听牌结构劫持重采样）。

**Q6：简历里"手牌推断与真实手牌吻合度约 95%"怎么算的？随机基线有意义吗？**

A：指标 = 真手牌每张牌的"推断期望张数"均值（合成对局里真手牌已知，可做真值验证）：完美 = 1.0，系统 ≈ 0.95。随机基线 ≈ 手牌数/牌池 = 13/119 ≈ 0.11——即完全无知、把概率均匀摊到剩余牌池时，每张真牌只能分到 11% 的期望质量。基线证明 0.95 不是虚高、指标有区分度；简历里删掉基线是为了好读，被追问时用 0.11 反证严谨性。

**Q7：冷启动预热/P99 长尾是什么？为什么简历里没写？**

A：冷启动（模型加载 + CUDA 初始化 + 跟踪器首次分配）实测可达 ~100s；真实对局中"停顿后再打"的首帧会撞上冷启动，识别 P99 长尾 2.5s，对实时辅助就是肉眼可见的卡顿。修复：启动时用黑帧跑一次完整推理完成预热，对局内每帧耗时恢复稳定（P50≈123ms）。这是 profiling 驱动的常规工程优化、不是算法亮点，所以简历只保留 ROI 精修这条真正的性能提升（单帧 86.6ms → 69.6ms）。
