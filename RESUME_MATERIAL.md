# 简历素材 —— 麻将AI助手 (Mahjong AI Assistant)

> 项目一句话:实时麻将辅助决策系统 —— 通过"看"真实牌桌画面,重建完整牌局状态,
> 推断三家对手隐藏手牌与听牌威胁,并给出牌效最优的出牌建议(只读辅助,不操作游戏)。
>
> 注:本项目为单机桌面应用(非微服务架构),所有"量化指标"均为代码/文档中实测数据;
> 代码中不存在的数字一律用 `[待补充]` / `[具体数字]` 占位,请按真实经历填写。

---

## 一、简历 Bullet Points(3-4 条,动作动词 + 量化结果开头)

**1. 视觉识别(CV 管线)**
搭建「YOLOv8m 检测 + ByteTrack 跟踪 + ROI 特写分类」两级识别管线,将牌河小目标召回率从 74% 提升至 85%(precision 98%),通过分级精修将单帧识别耗时降低 20%、帧率提升 25% 至 14.4fps,并用启动预热消除 P99=2.5s 的冷启动长尾。

**2. 概率推断(算法)**
实现 3 家 × 400 粒子的粒子滤波对手建模与软观测快照回放机制,对手隐藏手牌成分识别准确率约 95%(随机猜测基线仅 11%),听牌概率输出经校准(预测 6 成与实际约 6 成),打出牌到听牌率更新延迟 0.5~1.5s。

**3. 实时架构(工程)**
设计「事件流 + 行动者轮转自动机 + beam search」全局解码架构,将不可靠的视觉信号转化为事件流并做时间约束联合解码,在检测漏检/误检下事件流不中断、不连锁错位,信息显示滞后从 1~2 分钟压缩至 ~15s,主循环摸牌到出牌建议显示延迟 ~1s。

**4. 工程化质量(可选第 4 条)**
交付 363 项自动化测试(pytest 全绿,80s 跑完)与 mypy strict/ruff 零告警,自建「采集→标注→训练→评估→基准」数据闭环,沉淀 8 局真实对局基准报告,以数据驱动定位并优化识别管线(单帧识别 86.6ms → 69.6ms)。

---

## 二、最具技术深度的 3 个设计决策(STAR 格式"项目职责与难点")

### 决策①:两级识别 —— 检测器低阈值召回 + 分类器高精度过滤

> **情境(S)**:视频检测中牌河牌面仅 ~15px,检测器全局 recall 仅 74%,小目标召回更低;单纯调低检测阈值虽可把召回提到 85%,却引入大量 UI 元素/残影误检,precision 不可用。
> **任务(T)**:在不牺牲精度的前提下找回漏检的牌河牌,实现"高召回 + 高精度"兼得。
> **行动(A)**:在同一帧上跑两级模型——检测器(yolov8m)以 0.15 低阈值全图找框(高召回),对每个框裁剪放大 2 倍后由 ROI 分类器(yolov8n-cls,val 准确率 98.3%)做特写分类,误检框被分类器"背景类"直接滤掉;并增加分级精修(conf≥0.8 的高置信框跳过精修,实测仅 2.8% 会被改判),把 ROI 成本从每帧 ~25ms 降至 ~5ms(-80%)。
> **结果(R)**:牌河 recall ~85% @ precision 98%;单帧识别总耗时 86.6ms → 69.6ms(-20%),帧率 11.5 → 14.4fps(+25%);分级 vs 全量精修类别一致率 97.3%。

### 决策②:事件流 + 时间约束全局解码(轮转自动机 + beam search + 软归属翻案)

> **情境(S)**:单帧检测不可能完美(漏检、误检、帧间抖动),把每帧检测当真相的状态机几分钟就崩;且四家牌河在画面中央交错、物理不可分,固定区域划分方案既误判又绑定 UI 坐标、无法迁移线下。
> **任务(T)**:从"像素"可靠还原"谁打了什么",且对检测错误免疫。
> **行动(A)**:确立"检测输出不是真相,只是候选信号"的第一原则——事件生成层用多信号交叉确认(如"消失"必须同河其他牌还在才成立)把连续帧抽象为事件流;归属解码层用行动者轮转自动机做全局强先验(手牌 13→14/14→13 为相位硬锚,漂移不可能累积超过一轮)+ ByteTrack 身份跟踪 + beam search(窗口 10/束宽 16)联合解码,证据不足保留软归属,新证据到达时 8s 窗口内快照回放精确修正历史,而不是推翻重来。
> **结果(R)**:真实对局中手牌检测塌陷时事件流不中断、被碰/漏检不产生连锁错位;时间冻结机制把显示滞后从 1~2 分钟压到 ~15s;摸牌到显示延迟 ~1s,主循环零卡顿。

### 决策③:对手手牌是不可观测量的贝叶斯推断(粒子滤波 + 软观测 + 快照回放)

> **情境(S)**:对手手牌永远不可观测,只能从"他打了什么、没打什么"反推;且归属层存在不确定(约 15% 概率级错误),硬观测错位会直接毒化粒子滤波。
> **任务(T)**:实时输出三家听牌概率/最可能等待牌/放铳率,且对归属不确定性鲁棒。
> **行动(A)**:用粒子滤波(3 家 × 400 粒子)近似后验分布,以"向听数边际价值 + 孤张因子"建模理性玩家打出似然,并以 exp(-β·向听数)结构化漂移模拟"玩家在朝听牌构建";归属不确定时按概率对部分粒子更新(贝叶斯混合),归属翻案时回退到该观测的快照、按新分布确定性重放后续全部观测(RNG 状态随快照保存);全部推理放后台线程,主循环入队零阻塞,队列软上限 32 防无界积压。
> **结果(R)**:对手手牌成分识别约 95%(随机猜仅 11%);听牌概率校准良好(说 6 成就是真的约 6 成);对手真听牌时猜中听什么牌约 3~5 成;打出 → 听牌率更新 0.5~1.5s,单事件快照仅 ~0.4ms。

---

## 三、核心痛点(人工操作耗时 / 出错率)

本项目替代的人工工作 = **盯屏记录四家出牌 + 心算牌效 + 凭经验判断听牌与放铳风险**:

| 痛点 | 人工状态 | 系统状态 |
|---|---|---|
| 记录牌河/剩余张数 | 需全程盯屏逐张记录,易漏看错记 | 自动检测跟踪,牌河 recall ~85% @ precision 98% |
| 向听数/有效进张计算 | 涉及多牌型(标准/七对/十三幺)枚举,人工无法实时心算 | 毫秒级计算(10~20ms/手,lru_cache 加速) |
| 对手听牌/放铳判断 | 纯经验,信息不完备,误差大(随机基线手牌成分猜中仅 11%) | 粒子滤波概率输出,听牌概率经校准 |
| 复盘 | 回看录像逐帧核对 | 每局事件流 JSONL 自动落盘,结算自动核对 |

**量化占位(代码中无此数据,请自行补充)**:人工完整记录并判断一局(约 [具体数字] 巡)耗时约 [具体数字] 分钟/局;凭经验判断对手听牌与放铳风险的平均出错率约 [具体数字]%;引入系统后,单次决策时间降至 [具体数字] 秒,判断出错率降至 [具体数字]%。

---

## 四、系统全景(模块 / 上下游)

**形态说明**:本项目是**单机桌面应用**,非微服务;共 6 个核心模块 + 13 个工程脚本,无网络调用、无外部 SaaS 依赖。

| 模块 | 职责 | 体量 |
|---|---|---|
| `mahjong_cv` | 截屏捕获(Win32)、YOLO 检测 + ByteTrack 跟踪、ROI 分类精修、聚类 | ~1.1k 行 |
| `mahjong_ai/pipeline` | 事件提取(多信号交叉确认)、归属解码(轮转自动机 + beam search)、副露推断 | ~1.7k 行 |
| `mahjong_ai/inference` | 粒子滤波对手建模(3 家)、软观测 + 快照回放翻案 | ~0.8k 行 |
| `mahjong_ai/efficiency` | 向听数计算(标准/七对/十三幺)、有效进张出牌推荐 | ~0.5k 行 |
| `mahjong_engine` | 可扩展规则引擎(胡/听/动作判定,接口 + 注册表,多玩法) | ~0.5k 行 |
| `mahjong_ui` | PySide6 独立信息面板(三档自适应布局 + 降级链) | ~1.0k 行 |
| `scripts/` | 实时运行入口、离线评估、数据采集/标注/训练链、基准 | ~3.9k 行 |

**上下游依赖**:
- 上游(外部):欢乐麻将 PC 客户端(唯一数据源,只读截屏);模型权重(训练链产物,`data/models/` 固定路径,热替换生效);`config/river_regions.json` 区域种子配置
- 下游(输出):PySide6 实时面板(手牌/四家牌河/副露/听牌威胁/推荐);`data/settle_logs/game_*.jsonl` 事件日志(复盘 + `settle_check.py` 结算核对)
- 质量闭环:363 项 pytest + mypy strict + ruff 全绿;`docs/BENCHMARK.md` 真实对局基准

---

## 五、项目背景与成果(段落,降本增效,数值留空)

为解决麻将对局中**人工盯屏记录、心算牌效、凭经验判断对手听牌与放铳风险**效率低、易出错的问题,独立设计并实现了「麻将AI助手」—— 基于计算机视觉与概率推断的实时辅助决策系统。系统以约 [具体数字]fps 从游戏画面中重建完整牌局状态:通过"检测-跟踪-分类"两级视觉识别(牌河召回率提升至 85%、识别精度 98%)将视觉信号抽象为可靠事件流,再以粒子滤波(3 家 × 400 粒子)对三家对手隐藏手牌做贝叶斯推断,实时输出听牌概率与放铳预警。上线后,人工单局信息记录与判断耗时由约 [具体数字] 分钟压缩至约 [具体数字] 秒(降幅 [具体数字]%),凭经验判断的出错率由约 [具体数字]% 降至 [具体数字]%,决策延迟稳定在 ~1 秒,主循环零卡顿。项目沉淀 363 项自动化测试与完整的"采集-标注-训练-评估-基准"数据闭环,可快速复用于其他牌桌与线下场景。

---

## 六、请求全链路重组 + 架构亮点 + 专业技能短语

### 6.1 请求全链路(单机版"服务化"重组)

> 诚实说明:本系统为单机桌面应用,无 Nginx/网关/MQ/DB。下表将其内部 5 层流水线
> 按"全链路"句式重组,括号内为与微服务术语的**对应关系(类比)**,可用于面试表达,
> 但**不建议在简历中直接写"使用了 MQ/微服务"**。

```
欢乐麻将 PC 客户端(上游系统 / 数据源)
  │  Win32Capture 截屏(入口网关:后台线程, 最新帧优先, 冷启动预热)
  ▼
① 视觉识别服务  YOLOv8m 检测 + ByteTrack 跟踪 + ROI 特写分类(= 感知服务A)
  │  输出: 每帧 ~60 个牌面框(牌面 / 位置 / 跨帧身份)
  ▼
② 事件总线      多信号交叉确认 → 可靠事件流(= 消息队列 MQ: 解耦, 容忍单帧错误)
  │  输出: 牌出现 / 牌河消失 / 手牌数变化 / 副露信号
  ▼
③ 归属解码服务  行动者轮转自动机 + beam search + 软归属/翻案(= 服务B: 归属服务)
  │  输出: 每一张牌属于哪一家
  ▼
④ 对手推断服务  粒子滤波 × 3 家(牌效似然 + 软观测 + 快照回放)(= 服务C: 推断服务)
  │  输出: 听牌概率 / 等待牌 / 放铳率
  ▼
⑤ 策略服务      向听数 + 有效进张出牌推荐(副露展开评估)
  │  输出: 推荐牌 + 理由 + 危险牌预警
  ▼
自适应 UI 面板 + JSONL 事件日志落盘(data/settle_logs = 复盘数据库, 下游系统)
```

### 6.2 架构层面亮点

1. **全链路降级容错(容灾策略)**:每一层都有降级链——ROI 分类器缺失→跳过精修直接用检测类别;区域配置缺失→空间证据中性化、由轮转先验主导;手牌检测塌陷→"未知槽"兜底显示 + 手牌带锚定拒绝带外误判;单帧推理异常不杀主循环(5s 节流只记一次日志)。以"宁可空、不可错"保证系统不崩、不错。
2. **线程级并发与背压控制(横向扩展的本地形态)**:四线程架构(截图/检测推理/主循环/软观测 worker),检测侧"最新帧优先"丢弃积压帧,纯 Python 循环每 50 次迭代主动让出 GIL 防主循环饿死,观测队列软上限 32 防无界积压——把最坏延迟从"无界"压到 ~32 条观测。
3. **灰度/热更新与数据闭环**:模型输出统一落在 `data/models/` 固定路径,重训产物替换即生效(类"灰度切换");自建采集→标注→训练→评估脚本链 + 真实对局 JSONL 日志 + 离线基准(`bench_profile.py` → BENCHMARK.md),性能优化全部有数据依据(如 ROI 分级精修 -80% 耗时、预热消除 P99 长尾)。
4. **可观测性内置**:每帧诊断日志(帧耗时/手牌塌陷根因/推断快照/放铳风险)落盘,支持事后复盘与根因定位。

### 6.3 强动词短语(简历"专业技能"印证,以"基于/利用"开头)

1. **基于**「检测-跟踪-分类」两级视觉识别架构(YOLOv8m + ByteTrack + ROI 特写分类),以低阈值召回 + 背景类过滤实现牌河小目标"高召回 + 高精度"(85% @ 98%),并将单帧识别耗时优化 20%。
2. **基于**「事件流 + 时间约束全局解码」范式(轮转自动机 + beam search + 软归属快照回放),在检测漏检/误检下实现毫秒级状态重建,显示延迟压缩 90%(1~2 分钟 → ~15s)。
3. **利用**粒子滤波与贝叶斯混合推断,对不可观测状态(对手手牌/听牌/放铳风险)实时建模,手牌成分识别准确率约 95%(随机基线 11%),推断延迟 0.5~1.5s。
4. **利用**多线程并发 + 背压控制 + 全链路降级容错,保证 15fps 级主循环零卡顿与系统级鲁棒性(363 项测试全绿)。

---

## 附:可用量化指标清单(均出自代码/文档,面试可背)

- 牌河 recall 74% → ~85%;precision 98%;ROI 分类器 val 98.3%
- 单帧识别 86.6ms → 69.6ms(-20%);ROI 精修 24.9ms → 5.0ms(-80%);帧率 11.5 → 14.4fps(+25%)
- 冷启动 100s → 预热消除;识别 P99 长尾 2.5s(基准:8 局 482 条帧记录)
- 对手手牌成分识别 ~95%(随机猜 11%);听牌概率校准;猜中听牌 3~5 成
- 摸牌 → 显示 ~1s;打出 → 听牌率更新 0.5~1.5s;显示滞后 1~2 分钟 → ~15s
- 粒子:3 家 × 400;beam search 窗口 10 / 束宽 16;事件快照 ~0.4ms;粒子更新 100~300ms
- 出牌推荐 10~20ms/手;363 项 pytest 全绿(80s);mypy strict / ruff 零告警
- 代码规模:src ~5.9k 行 + tests ~4.7k 行 + scripts ~3.9k 行(共 ~14.5k 行)
